Array.from()
可以将两种对象转换成数组。
1.部署了Iterator接口的对象，比如：Set，Map，Array。
2.类数组对象，什么叫类数组对象，就是一个对象必须有length属性，没有length，转出来的就是空数组。

Array.of()
用于将一组值，转换为数组。

copyWithin()
在当前数组内部，将指定位置的成员复制到其他位置（会覆盖原有成员），然后返回当前数组。也就是说，使用这个方法，会修改当前数组。

entries()   下标和值都能得到
keys()     只能得到下标
values()   只能得到值

fill()
使用给定值，填充一个数组。
还可以接受第二个和第三个参数，用于指定填充的起始位置和结束位置。

map() 映射

filter为过滤、筛选,指数组filter后，返回过滤后的新数组。用法跟map极为相似

reduce() 数组中的每个值（从左到右）开始合并，最终为一个值。

JSON.parse() 方法用来解析JSON字符串，构造由字符串描述的值或对象。
JSON.stringify() 方法是将一个值(对象或者数组)转换为一个 JSON字符串，














